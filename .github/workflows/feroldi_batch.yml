name: Feroldi Batch Recolectar

# Kika ejecuta esto. El código es responsabilidad de @patonet.
# Disparo manual desde GitHub Actions → Actions → Feroldi Batch Recolectar → Run workflow

permissions:
  contents: write   # necesario para que Kika pueda hacer git push

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run? escribir exactamente: true  o  false'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'
      sleep:
        description: 'Segundos entre tickers (default 3)'
        required: false
        default: '3'
        type: string

jobs:
  batch:
    runs-on: ubuntu-latest
    timeout-minutes: 30      # 50 tickers × 3s + overhead ≪ 30 min

    env:
      FEROLDI_OUTPUT_DIR: ${{ github.workspace }}/kika/outputs

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install edgartools yfinance

      - name: Run batch collector
        run: |
          FLAGS=""
          if [ "${{ inputs.dry_run }}" = "true" ]; then FLAGS="$FLAGS --dry-run"; fi
          FLAGS="$FLAGS --sleep ${{ inputs.sleep }}"
          mkdir -p kika/outputs
          python kika/feroldi_batch_recolectar.py $FLAGS

      - name: Upload batch JSON como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: batch-datos-run${{ github.run_number }}
          path: kika/outputs/batch_datos*.json
          retention-days: 90

      - name: Commit resultado al repo
        if: ${{ inputs.dry_run != 'true' }}
        run: |
          git config user.name  "Kika"
          git config user.email "kika@feroldi.bot"
          git add kika/outputs/
          git diff --staged --quiet && echo "Sin cambios" || \
            git commit -m "kika: batch run #${{ github.run_number }}"
          git push

      - name: Notificar por Telegram
        if: always()
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
        run: |
          STATUS="${{ job.status }}"
          ICON="✅"
          [ "$STATUS" != "success" ] && ICON="❌"
          DRY=""
          [ "${{ inputs.dry_run }}" = "true" ] && DRY=" [DRY-RUN]"
          MSG="${ICON} Kika batch #${{ github.run_number }}${DRY} → ${STATUS}"$'\n'"Artefacto: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          curl -s -X POST "https://api.telegram.org/bot${TG_TOKEN}/sendMessage" \
            -d chat_id="${TG_CHAT_ID}" \
            -d text="${MSG}" \
            -d parse_mode="HTML" || echo "Telegram notify failed (non-fatal)"
